# To fix "Unknown env config devdir" (often set by Cursor/IDE):
# - Add to ~/.zshrc or ~/.bashrc: unset npm_config_devdir
# - Or run without the warning: node scripts/run.js run build (same for dev, lint, test:run)
